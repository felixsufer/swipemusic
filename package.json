{
  "name": "swipesound",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "npm install --prefix server && cd client && npm install --legacy-peer-deps && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
