[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix server", "cd client && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "node server/index.js"
