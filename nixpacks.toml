[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = [
  "npm install --prefix server --no-cache",
  "cd client && npm install --legacy-peer-deps --no-cache"
]

[phases.build]
cmds = ["cd client && CI=false npm run build"]

[start]
cmd = "node server/index.js"
